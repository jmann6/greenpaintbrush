
/* Allows user to paint only when mouse is pressed.  */
var draw = function() {
    noStroke();
    fill(15, 70, 15);  /* dark green*/
    if (mouseIsPressed){
        ellipse(mouseX, mouseY, 50, 50);  /*increased size of paint stroke*/
    }
};
